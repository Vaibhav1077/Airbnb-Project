# API base URL for the backend
# In development: leave empty or unset — Vite proxy handles /api requests to localhost:3000
# In production: set to your deployed backend URL (e.g. https://api.yoursite.com/api)
VITE_API_URL=
